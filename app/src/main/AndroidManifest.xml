<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Exact alarms are used to start the foreground service when a direct
         startForegroundService() is denied by Android 15/16 BFSL restrictions.
         USE_EXACT_ALARM is auto-granted (sideloaded app), so the bypass works
         without the user toggling a setting. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <!-- Lets the plugin ask to be exempted from battery optimization so Android
         won't freeze/Doze the process and block service starts at FETCH time. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <queries>
        <package android:name="com.highwayradar.app" />
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="CHP + Waze SABRE"
        android:theme="@android:style/Theme.Material.Light">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Foreground launch escape hatch: HR launches this (advertised as
             alternative_startup_activity) to bring the plugin to the foreground
             so the service can start without BFSL restrictions. -->
        <activity
            android:name=".AltStartupActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <!-- Auto-start service after device boot -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- SABRE Broadcast Receiver -->
        <receiver
            android:name=".MainBroadcastReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="app.sabre.HANDSHAKE" />
                <action android:name="app.sabre.wzsabre.HANDSHAKE" />
                <action android:name="app.sabre.wzsabre.FETCH_REQUEST" />
                <action android:name="app.sabre.wzsabre.SUBMIT_REPORT" />
                <action android:name="app.sabre.wzsabre.CONFIRM_REPORT" />
                <action android:name="app.sabre.wzsabre.DISCARD_REPORT" />
                <action android:name="app.sabre.wzsabre.SHUTDOWN" />
            </intent-filter>
        </receiver>

        <!-- Foreground Service -->
        <service
            android:name=".SabreService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
    </application>
</manifest>